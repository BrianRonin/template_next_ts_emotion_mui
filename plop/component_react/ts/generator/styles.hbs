import styled, { css, DefaultTheme } from 'styled-components'
import { {{ camelCase name }}Props } from './{{ snakeCase name }}'__prop_style__

export const Main = styled.{{ htmlElement }}__type_style__`
  ${({ theme }) => css`
    //
  `}
`
